CREATE DATABASE gisdb;
CREATE SCHEMA gis;
CREATE EXTENSION postgis;
SELECT postgis_full_version();

http://www.demarcation.org.za/site/shapefiles/
Or use the data on the workshop that you have downloaded.
'/home/Downloads/Postgis...'

Use shp2pgsql

shp2pgsql -s 4326 -l

The -s 4326 is for the South Africa SRID(Spatial Referential ID)

SRID - (Spatial Referential ID)
Sphere to flat surface
Most common Web Mercator
Managing and comparing data across projections

# SELECT * FROM spatial_ref_sys


# Indexing the data
For spatial indexes - use a GiST index.
Stores bounding box of the geometry as the index
Remenber R-trees

CREATE INDEX idx_road_geom ON road USING gist(the_geom);

shp2pgsql -s 4326 -I 'file' gis.roads | psql -d gisdb -U fuhr






